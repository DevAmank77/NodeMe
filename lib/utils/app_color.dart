import 'package:flutter/material.dart';

class AppColors {
  // Primary and Secondary
  static const Color primary = Color(0xFF3DA9FC);
  static const Color secondary = Color(0xFF9B5DE5);

  // Status Colors
  static const Color success = Color(0xFF70E000);
  static const Color warning = Color(0xFFFFBE0B);
  static const Color error = Color(0xFFFF4D6D);

  // Backgrounds and Surfaces
  static const Color background = Color(0xFFF7F9FC);
  static const Color card = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textMuted = Color(0xFF5A5A5A);

  // Graph Edge Line (Optional)
  static const Color graphLine = Color(0xFFB0BEC5);
}
